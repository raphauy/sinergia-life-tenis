# Deploy Notes

Notas de deploy por feature/release: pasos manuales, migraciones que requieren orden específico, variables de entorno nuevas, validaciones post-deploy.

Convención: `<feature>-<contexto>.md` (ej: `la-escalera-fase1.md`).
